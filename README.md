P2P UDP ChatRoom App

## INFO
* Implemented in python 2.7
* execute the chat.py program to start the chat room
* The default port is 1025, you can change it with the -p flag
* see /chat.py -h for help
